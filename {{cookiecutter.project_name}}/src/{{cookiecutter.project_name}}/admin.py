from django.contrib import admin
from . import models as m


# Register your models here.
# @admin.register(m.PathRule)
# class PathRuleAdmin(admin.ModelAdmin):
#     list_display = [f.name for f in m.PathRule._meta.get_fields()]
